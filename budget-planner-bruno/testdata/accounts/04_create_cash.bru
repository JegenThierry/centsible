meta {
  name: 04_create_cash
  type: http
  seq: 4
}

post {
  url: http://localhost:8080/api/budget-accounts
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Cash Wallet",
    "initialBalance": 150.00,
    "currency": "EUR"
  }
}

script:post-response {
  if (res.getStatus() === 200) {
    const body = res.getBody();
    if (body && body.id) {
      bru.setVar("accountCashId", body.id);
    }
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
