meta {
  name: forgot-password
  type: http
  seq: 5
}

post {
  url: http://localhost:8080/api/auth/forgot-password
  body: json
  auth: inherit
}

body:json {
  {
    "username": "{{username}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
