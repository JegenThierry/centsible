meta {
  name: reset-password
  type: http
  seq: 6
}

post {
  url: http://localhost:8080/api/auth/reset-password
  body: json
  auth: inherit
}

body:json {
  {
    "token": "{{passwordResetToken}}",
    "password": "{{password}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
